/**
 * Participantes que no reciben la penalización «apuesta catastrófica» (-1 pt) en fútbol.
 * Importado desde `src/futbol-utils.js` y `lib/scoring.mjs` (misma fuente).
 */
export const FUTBOL_CAT_PENALTY_EXCLUDED_USERS = new Set(["Paula"]);
